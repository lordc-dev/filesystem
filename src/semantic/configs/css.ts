import type { LanguageConfig } from "../language-config-types.js";

export const cssConfig: LanguageConfig = {
  symbolNodes: {},
  commentTypes: ["comment"],
  decoratorTypes: [],
  stringTypes: ["string_value"],
};